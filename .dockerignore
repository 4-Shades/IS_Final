# Allowlist: images only need the application code and requirements.
# Everything else (infra/ with Terraform provider binaries, tests, docs, envs) stays out of the build context.
*
!agents/
!common/
!shared/
!travel_ui.py
!requirements/

**/__pycache__
**/*.py[cod]
